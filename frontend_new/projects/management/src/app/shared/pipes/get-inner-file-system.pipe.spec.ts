import { GetInnerFileSystemPipe } from './get-inner-file-system.pipe';

describe('GetInnerFileSystemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetInnerFileSystemPipe();
    expect(pipe).toBeTruthy();
  });
});
