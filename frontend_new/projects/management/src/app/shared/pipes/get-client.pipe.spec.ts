import { GetClientPipe } from './get-client.pipe';

describe('GetClientPipe', () => {
  it('create an instance', () => {
    const pipe = new GetClientPipe();
    expect(pipe).toBeTruthy();
  });
});
