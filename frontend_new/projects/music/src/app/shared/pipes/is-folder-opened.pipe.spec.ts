import { IsFolderOpenedPipe } from './is-folder-opened.pipe';

describe('IsFolderOpenedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsFolderOpenedPipe();
    expect(pipe).toBeTruthy();
  });
});
