import { FilterFolderTreePipe } from './filter-folder-tree.pipe';

describe('FilterFolderTreePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFolderTreePipe();
    expect(pipe).toBeTruthy();
  });
});
