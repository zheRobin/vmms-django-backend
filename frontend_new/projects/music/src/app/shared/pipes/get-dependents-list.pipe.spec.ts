import { GetDependentsListPipe } from './get-dependents-list.pipe';

describe('GetDependentsProgramListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDependentsListPipe();
    expect(pipe).toBeTruthy();
  });
});
