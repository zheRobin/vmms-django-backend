import { GetFilterTypePipe } from './get-filter-type.pipe';

describe('GetFilterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFilterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
