import { GetFormControlPipe } from './get-form-control.pipe';

describe('GetFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
