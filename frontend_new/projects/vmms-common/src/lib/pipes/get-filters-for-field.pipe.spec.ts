import { GetFiltersForFieldPipe } from './get-filters-for-field.pipe';

describe('GetFiltersForFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFiltersForFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
