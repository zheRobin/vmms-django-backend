import { FilterItemListPipe } from './filter-item-list.pipe';

describe('FilterItemListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemListPipe();
    expect(pipe).toBeTruthy();
  });
});
