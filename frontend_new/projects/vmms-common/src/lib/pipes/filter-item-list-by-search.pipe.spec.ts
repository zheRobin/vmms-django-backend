import { FilterItemListBySearchPipe } from './filter-item-list-by-search.pipe';

describe('FilterItemListBySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemListBySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
